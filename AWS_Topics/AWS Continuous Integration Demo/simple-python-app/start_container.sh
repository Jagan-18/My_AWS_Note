#!/bin/bash
set -e

# Pull the Docker image from Docker Hub
echo

# Run the Docker image as a container
echo "Starting the Docker container..."



#docker pull simple-python-app:latest
#docker run -d -p 5000:5000 simple-python-app "simple-python-app:latest"
#echo "Docker container started successfully." "Pulling the Docker image..."
#docker pull simple-python-app:latest
#echo "Docker image pulled successfully." "Pulling the Docker image..."
#docker pull simple-python-app:latest
#echo "Docker image pulled successfully."
#docker run -d -p 5000:5000 simple-python-app:latest
#echo "Docker container started successfully."               