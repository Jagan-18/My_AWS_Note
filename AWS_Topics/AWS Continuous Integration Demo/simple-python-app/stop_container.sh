#!/bin/bash
set -e

# Stop the running container (if any)
echo "Hi" "Stopping the Docker container..."


#docker pull simple-python-app:latest || true
#docker run -d -p 5000:5000 simple-python-app:latest || true
#echo "Docker container started successfully."
##echo "Pulling the Docker image..."
#docker pull simple-python-app:latest || true
#echo "Docker image pulled successfully."
#echo "Stopping the Docker container..."
#docker stop simple-python-app || true
#echo "Docker container stopped successfully."